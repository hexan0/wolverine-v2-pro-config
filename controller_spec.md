### UUID

**`52401524-f97c-7f90-0e7f-6c6f4e36db1c`**

### ボタンID
機能 / ボタン,ID (16進数),備考
× (Cross / A / ボタン位置下),0x20,右アクション
○ (Circle / B / ボタン位置右),0x21,右アクション
□ (Square / X / ボタン位置左),0x22,右アクション
△ (Triangle / Y / ボタン位置上),0x23,右アクション
L1 (バンパー),0x24,
R1 (バンパー),0x25,
L2 (トリガー),0x26,
R2 (トリガー),0x27,
L3 (左押し込み),0x28,
R3 (右押し込み),0x29,
↑ (Up),0x2c,十字キー
↓ (Down),0x2d,十字キー
← (Left),0x2e,十字キー
→ (Right),0x2f,十字キー
Create (Share),0x34,
Options (Start),0x35,
クラッチ(両方),0x36,感度低下機能
クラッチ(左),0x37,感度低下機能
クラッチ(右),0x38,感度低下機能

### デフォルト設定
M1:□
M2:△
M3:×
M4:○
M5:クラッチ左
M6:クラッチ右
感度クラッチ左：小さい方から6段階目
感度クラッチ右：小さい方から6段階目
デッドゾーン左：小さい方から4段階目（最大）
デッドゾーン右：小さい方から4段階目（最大）
Chroma：スペクトラム

### 1. ボタンマッピング（M1〜M6）の構造

* **パケット法則:** `0a 00 00 08 04 01 00 01 [対象ボタンID] 00 10 01 [機能ID] 00 00 00 00`

変更対象のボタンID
* `10`: M1
* `11`: M2
* `12`: M3
* `13`: M4
* `14`: M5
* `15`: M6

`[機能ID]`（`21`, `22`, `23`, `36`など）が、AボタンやBボタンなどの具体的なキー割り当て、ボタンID

### 2. 感度クラッチ（10段階）の構造

* **パケット法則:** `01 00 00 09 02 01 [スティックID] [感度値]`

* **スティックID:** `01` が左スティック、`02` が右スティック
* **感度値:** `01`（最小）から徐々に上がり、`63`（最大）に到達しています。16進数の `63` は10進数で「99」となるため、1〜99までの間で約11刻みで10段階を表現している

### 3. デッドゾーン（4段階）の構造

* **パケット法則:** `01 00 00 09 08 01 [スティックID] [デッドゾーン値]`

* **スティックID:** 感度クラッチと同じく `01` が左、`02` が右
* **デッドゾーン値:** `01`（最小）、`03`、`05`（デフォルト）、`07` の4段階で設定されている。

`00`や`50`といった値でも設定が反映されていることを確認
目安として`50`で傾きのうち80%程度がデッドゾーンとなる

### 4. Chroma（ライティング）の構造

* **パケット法則:** `0c 00 00 11 03 01 05 01 05 [発光モード] [速度/付加情報] [色情報1] [色情報2]`

* **発光モード:** `00`=オフ, `01`=スタティック, `02`=ブリージング, `03`=スペクトラム
* **色情報:** 指定された緑色（#44D62C）がそのまま `44 d6 2c` として組み込まれています。スペクトラムやオフの際は、ダミー値として `ff ff ff`（白）が埋め込まれています。

### 送信すべきデータ

[連番] + [送信パケット]
連番は01からスタートする16進数2桁
合計20バイト(ライティングの場合)

例）
[0a] + [0c 00 00 11 03 01 05 01 05 01 00 00 01 44 d6 2c ff ff ff]
10番目の送信、ライティングをスタティックのデフォルト緑に設定する
